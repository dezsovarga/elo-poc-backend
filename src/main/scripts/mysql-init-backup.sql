-- DROP DATABASE IF EXISTS elopoc;
-- DROP USER IF EXISTS `elo_manager`@`%`;
-- CREATE DATABASE IF NOT EXISTS elopoc CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- CREATE USER IF NOT EXISTS `elo_manager`@`%` IDENTIFIED WITH mysql_native_password BY 'password';
-- GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, REFERENCES, INDEX, ALTER, EXECUTE, CREATE VIEW, SHOW VIEW,
-- CREATE ROUTINE, ALTER ROUTINE, EVENT, TRIGGER ON `elopoc`.* TO `elo_manager`@`%`;
-- FLUSH PRIVILEGES;